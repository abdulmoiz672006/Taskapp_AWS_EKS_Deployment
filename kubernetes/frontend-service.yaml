apiVersion: v1
kind: Service
metadata:
  name: pern-frontend-service
  namespace: todo-app
spec:
  selector:
    app: pern-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort

